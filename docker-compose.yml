services:
  goapi:
    build: 
      dockerfile: Dockerfile
      context: .
    ports:
      - 8080:8080

  database:
    image: postgres:16-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=mypassword